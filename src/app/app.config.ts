// App-specific config

// Using CommonJS instead of ES2015+ export, because we also need to
// provide this object to Webpack in vue.config.js.
const config = {
	title: "GymKonnect",
	description: "All your gym needs at one place",
}

export default config
